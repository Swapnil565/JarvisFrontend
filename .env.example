# Backend API URL (required)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: Use mock API for development without backend
NEXT_PUBLIC_USE_MOCK_API=false

# Optional: Enable debug logging
NEXT_PUBLIC_DEBUG=false
